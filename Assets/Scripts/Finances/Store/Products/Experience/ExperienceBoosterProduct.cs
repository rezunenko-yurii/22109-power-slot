namespace StoreImp.Products.Experience
{
    /*public class ExperienceBoosterProduct : BoosterProduct
    {
        [field: SerializeField] public int Amount { get; private set; }

        public override event Action Purchased;
        public override ICurrency Price { get; protected set; }
        public override ProductType Type { get; protected set; }

        public override string Description()
        {
            return $"{amount}X experience for {hours} hours";
        }

        public override void Activate()
        {
            Booster.Add(amount, hours);
        }
    }*/
}